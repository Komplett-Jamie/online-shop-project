// subscribeToEvent("cartStateUpdated", function(state) {
//     let totalCartItems = state.items.reduce((total, n) => total + n.quantity, 0);
//     document.getElementById("small_cart").innerText = totalCartItems || 0;
// });